# cd ansible
# vim requirements.yml
---
- src: http://classroom/materials/haproxy.tar
  name: balancer
- src: http://classroom/materials/phpinfo.tar
  name: phpinfo

# ansible-galaxy install roles/requirements.yml
# ansible-galaxy  list
