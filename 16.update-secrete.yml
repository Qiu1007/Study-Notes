# - 当前的库密码为 insecure8sure
# - 新的库密码为 bbs2you9527

# wget http://classroom/materials/salaries.yml
# ansible-vault rekey --aks-vault-pass salaries.yml
# ansible-vault view salaries.yml

# ansible-vault view --ask-vault-pass salaries.yml
